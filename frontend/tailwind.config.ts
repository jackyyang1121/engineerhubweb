/** 
 * @deprecated Tailwind CSS v4.0 uses CSS-first configuration
 * Configure your theme in your CSS file using @theme instead
 */
// **@deprecated 標記：這個檔案在 Tailwind CSS v4.0 中已被棄用**
// **新的配置方式：v4.0 改用 CSS-first 配置，不再依賴 JavaScript 配置檔**

// This file is kept for backward compatibility but is not used in v4.0
// **向後相容：保留此檔案是為了相容性，但在 v4.0 中實際上不會被使用**

// Configure your theme in your main CSS file instead:
// **新配置位置：應該在主要的 CSS 檔案中配置主題**

export default {}; 
// **export default：這是 TypeScript 中的預設導出語法**
// **空物件：由於 Tailwind v4.0 不使用此檔案，所以導出空物件**
// **向後相容性：保留此檔案確保舊版本的工具不會報錯**
// **v4.0 新方式：實際配置應在 CSS 檔案中使用 @theme 指令**